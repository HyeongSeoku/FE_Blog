---
title: TEST 포스트 타이틀
description: 포스트에 대한 설명입니다.
category: DEV
createdAt: 2024.10.24
subCategory: FE
tags:
  - React
  - Next.js
---

[OpenAI 홈페이지](https://www.openai.com)

# Hello, MDX!

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};

const handleCopy = () => {
  navigator.clipboard.writeText(codeText).then(() => {
    setCopied(true);
    setTimeout(() => setCopied(false), 2000); // 2초 후 복사 완료 메시지 숨기기
  });
};
```

```typescript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = (text: string) => {
  console.log(text);
  return bar;
};

const handleCopy = () => {
  navigator.clipboard.writeText(codeText).then(() => {
    setCopied(true);
    setTimeout(() => setCopied(false), 2000); // 2초 후 복사 완료 메시지 숨기기
  });
};
```

# Hello, MDX!112321421421421kjlsdkvjsdaklvjskldvjsdlkvjsdalvjsdklvjs

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```python
from typing import Final

# 작명 규칙을 이용한 상수 표기
HELLO = "world!"
# 작명 규칙과 타입 힌트를 이용한 상수 표기
HELLO_WORLD: Final = "hello, world!"

HELLO = 3
```

# Hello, MDX!2

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```

# Hello, MDX!3

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```

# Hello, MDX!4

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```

# Hello, MDX!5

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```

# Hello, MDX!6

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```

# Hello, MDX!7

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```

# Hello, MDX!8

This is a sample MDX file with **Markdown** and <span style={{ color: 'red' }}>JSX</span>.

## MDX Test File

> 강조 테스트

<br />

This is a test file

```javascript
const foo = "test";
console.log(foo);

const bar = "bar";

const barFunc = () => {
  return bar;
};
```
