현재 프로젝트의 TypeScript 타입 에러를 모두 찾아서 수정해.

## 순서

1. `npx tsc --noEmit 2>&1` 실행해서 에러 전체 목록 파악
2. 에러를 파일별로 그룹핑
3. 각 에러의 원인 분석 후 수정
4. 수정 후 `npx tsc --noEmit`으로 재확인

## 수정 원칙

- 타입 단언(`as`) 사용 금지 — 불가피한 경우에만 `unknown` 경유
- `any` 사용 금지
- 타입 가드로 타입을 좁힐 것
- 인터페이스/타입 정의가 누락됐다면 추가
